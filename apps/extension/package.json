{
  "name": "@zipkit/extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ZipKit Chrome Extension",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@zipkit/archive-core": "workspace:*",
    "@zipkit/archive-security": "workspace:*",
    "@zipkit/ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
